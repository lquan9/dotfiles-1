####################
# Variables
####################
# Save user-configs alias directory to environment variable if not already set
if [ -z "$USER_CONFIGS_ALIAS_PATH" ]; then
  export USER_CONFIGS_ALIAS_PATH="$(dirname $(readlink -f $0))"
fi
####################
####################



####################
# Logins and Configs
####################
alias src="exec ${SHELL}"
alias cdc="cd ${USER_CONFIGS_BASE_PATH}"

# zshrc
alias czsh="$EDITOR ${HOME}/.zshrc"
alias szsh="source ${HOME}/.zshrc"

# alias
alias cali="$EDITOR ${USER_CONFIGS_ALIAS_PATH}/alias"
alias sali="source ${USER_CONFIGS_ALIAS_PATH}/.alias"

# oh-my-zsh
alias cozsh="$EDITOR ${HOME}/.oh-my-zsh"
alias sozsh="source ${HOME}/.oh-my-zsh"

# powerlevel10k
alias cp10k="$EDITOR ${USER_CONFIGS_ZSH_PATH}/.p10k.zsh"
alias sp10k="source ${USER_CONFIGS_ZSH_PATH}/.p10k.zsh"

# tmux
alias ctmux="$EDITOR ${HOME}/.tmux.conf"
alias stmux="source ${HOME}/.tmux.conf"
alias ktmux="pkill -f tmux"
####################
####################



####################
# Common
####################
# navigation
alias e="exit"
alias cd..="cd .."
alias cd....="cd ../.."
alias cd/="cd /"

alias la="ls -lAohp --color=always"
alias lte="ls -lAohpt --color=always | awk 'NR != 1 && NR < 12'"

alias pa="ps -af"

alias font_refresh="sudo fc-cache -f -v"
####################
####################



# @todo Distro Package Manager Aliases
# @body Create package manager aliases for other distro (fedora, alpine, etc) package managers.
####################
# Package Managers
####################
# apt
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh apt && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.apt.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.apt.alias

# dpkg
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh dpkg && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.dpkg.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.dpkg.alias

# pip
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh pip3 && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.pip.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.pip.alias

# brew
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh brew && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.brew.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.brew.alias

# gem
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh gem && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.gem.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.gem.alias
####################
####################



####################
# Applications
####################
# chrome
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh google-chrome && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.chrome.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.chrome.alias

# httpie
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh http && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.httpie.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.httpie.alias

# hub
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh hub && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.hub.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.hub.alias

# k
[[ -f ${ZSH_CUSTOM:-${HOME}/.oh-my-zsh/custom}/plugins/k/k.plugin.zsh ]] && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.k.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.k.alias

# nmap
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh nmap && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.nmap.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.nmap.alias

# ssh
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh ssh && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.ssh.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.ssh.alias

# vim
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh vim && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.vim.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.vim.alias

# vscode
${USER_CONFIGS_BASE_PATH}/scripts/is_installed.sh code && [[ -f ${USER_CONFIGS_ALIAS_PATH}/.vscode.alias ]] && source ${USER_CONFIGS_ALIAS_PATH}/.vscode.alias
####################
####################
