# brew aliases
alias brew_install="brew install"
alias brew_update="brew update"
alias brew_upgrade="brew upgrade"
alias brew_clean="brew cleanup"
alias brew_info="brew info"

brew_list() {
    if [ "$1" != "" ]
    then
        brew list | grep -i $1
    else
        brew list
    fi
}
